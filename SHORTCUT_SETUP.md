# Polaris 捕捉ショートカット — iPhone 設定手順

## 完成イメージ
「ヘイSiri、タスク追加」→ 音声入力 → 「クリニック、第2領域、今週で登録しました。CR勉強会…」

---

## 必要なもの
- iPhone（iOS 16以降）
- ショートカットアプリ（標準搭載）
- GAS Web App が稼働していること（VITE_GAS_URLが取得済み）

---

## 手順

### 1. ショートカットを新規作成
「ショートカット」アプリ → 右上「＋」

### 2. アクションを追加（順番通り）

#### ① テキストを音声入力
- アクション検索: 「テキストを音声入力」
- 設定: 言語 = 日本語、停止 = 自動
- 出力変数名: 「タスクテキスト」

#### ② URLの内容を取得
- アクション検索: 「URLの内容を取得」
- URL: `（GAS Web App の /exec URL）`
- 方式: **POST**
- ヘッダー: `Content-Type: application/json`
- 本文: **JSON** を選択、以下のキーと値を追加
  - キー: `token` → 値: `（VITE_GAS_TOKEN と同じ文字列）`
  - キー: `action` → 値: `create`
  - キー: `text`  → 値: 変数「タスクテキスト」を選択
- 出力変数名: 「レスポンス」

#### ③ 辞書の値を取得
- アクション検索: 「辞書の値を取得」
- キー: `spoken`
- 辞書: 変数「レスポンス」

#### ④ テキストを読み上げ
- アクション検索: 「読み上げ」
- テキスト: 前のアクションの出力

### 3. ショートカット名を設定
「タスク追加」（Siriで「タスク追加」と呼び出せるようになる）

### 4. ホーム画面・アクションボタンへの追加
- ホーム画面: ショートカットを長押し → ホーム画面へ追加
- アクションボタン（iPhone 15 Pro以降）: 設定 → アクションボタン → ショートカット → 「タスク追加」

---

## テスト方法
1. Siriに「タスク追加」と言う
2. マイクが出たら「来週火曜の15時に機材選定で業者へ電話」と言う
3. Siriが「クリニック、第2領域、今週で登録しました。…」と読み上げたら成功
4. Google Tasks アプリを開き、「みわ歯科クリニック船橋」リストに `[第2] 15時に業者へ電話する` が入っていれば完了

---

## トラブルシューティング

| 症状 | 確認箇所 |
|------|---------|
| 読み上げが「内容が空でした」 | 音声入力が空だった → もう一度試す |
| 読み上げが「認証エラー」 | tokenが一致しているか確認 |
| 読み上げが「APIエラー」 | GASのデプロイが「アクセス：全員」になっているか確認 |
| ショートカットがエラーになる | JSON本文の `text` に変数「タスクテキスト」が正しく入っているか確認 |
| Googleタスクに追加されない | GASエディタで `testCreate()` を実行してログを確認 |
